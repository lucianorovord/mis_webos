name: Deploy Laravel Sail (Local)
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy (Local) con Sail
        run: |
          cd /home/lucci/Escritorio/mis_huevos
          git pull
